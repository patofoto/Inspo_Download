# Network drive path inside the container
# This should match the volume mount in docker-compose.yml
NETWORK_DRIVE_PATH=/downloads
API_KEY=your-secret-key-here

# Port the server listens on
PORT=3000

# Node environment
NODE_ENV=production
